#include "../libasm.h"

int	main(void)
{
	char	*str;
	char	*dst;
	int		len;
	void	*ptr;

	str = "Hellowdwdw";
	len = ft_strdup(str);
	// printf("%d\n", len);
	// printf("%s\n", dst);
	// dst = strdup(str);
	// printf("%s\n", dst);
	// free(dst);
	return (0);
}
